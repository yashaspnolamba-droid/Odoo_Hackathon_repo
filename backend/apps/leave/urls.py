"""Placeholder leave URLs — to be implemented by leave developer."""
from django.urls import path

app_name = "leave"

urlpatterns = [
    # TODO: Leave developer will add endpoints here
    # GET /api/v1/leave/types/ — list leave types
    # GET /api/v1/leave/balance/ — get leave balance
    # POST /api/v1/leave/requests/ — create leave request
    # PATCH /api/v1/leave/requests/{id}/approve/ — approve
    # PATCH /api/v1/leave/requests/{id}/reject/ — reject
    # PATCH /api/v1/leave/requests/{id}/cancel/ — cancel
]

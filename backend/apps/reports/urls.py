"""Placeholder reports URLs — to be implemented by reporting developer."""
from django.urls import path

app_name = "reports"

urlpatterns = [
    # TODO: Reporting developer will add endpoints here
    # GET /api/v1/reports/attendance/ — attendance report
    # GET /api/v1/reports/leave/ — leave report
    # GET /api/v1/reports/employees/ — employee report
    # GET /api/v1/reports/payroll/ — payroll report
    # All support filters: employee, department, designation, date_range, status
    # Export support: CSV, XLSX, PDF
]

"""Placeholder attendance URLs — to be implemented by attendance developer."""
from django.urls import path

app_name = "attendance"

urlpatterns = [
    # TODO: Attendance developer will add endpoints here
    # GET /api/v1/attendance/ — list attendance records
    # POST /api/v1/attendance/check-in/ — record check-in
    # POST /api/v1/attendance/check-out/ — record check-out
    # POST /api/v1/attendance/corrections/ — request correction
    # PATCH /api/v1/attendance/corrections/{id}/approve/ — approve correction
    # PATCH /api/v1/attendance/corrections/{id}/reject/ — reject correction
]
